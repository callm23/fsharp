//namespace global //use global namespace, no specific
namespace Call.Demo.Calculator

module Adder =
  let add a b = a + b
  let add5 x = x + 5

module Multiplier =
  let mult x y = x * y
  let square x = x * x // int -> int (int goes to int)

